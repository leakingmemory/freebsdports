--- Makefile.in.orig	2025-07-16 22:08:59 UTC
+++ Makefile.in
@@ -30,7 +30,7 @@ INSTALL = @INSTALL@
 LEXFLAGS = -t 
 
 INSTALL = @INSTALL@
-INSTALL_OWNER = -o root -g 0
+INSTALL_OWNER =
 
 prefix = @prefix@
 exec_prefix = @exec_prefix@
@@ -102,9 +102,6 @@ install_config: 
 
 install_config: 
 	$(INSTALL) -d -m 755 $(INSTALL_OWNER) $(DESTDIR)$(ETC_DIR)
-	if [ ! -f $(ETC_DIR)/vtunngd.conf ]; then \
-	  $(INSTALL) -m 600 $(INSTALL_OWNER) vtunngd.conf $(DESTDIR)$(ETC_DIR); \
-	fi
 
 install_lib:
 	$(INSTALL) -d -m 755 $(INSTALL_OWNER) $(DESTDIR)$(LIB_DIR)
